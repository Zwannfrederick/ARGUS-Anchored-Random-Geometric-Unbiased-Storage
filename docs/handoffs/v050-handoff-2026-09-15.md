# ARGUS v0.5 oturum devri — 2026-09-15

## Devam oturumu güncellemesi — önce bu bölümü oku

Aşağıdaki eski devir kaydı tarihsel kaldı. Native blok attention kodu ve UI-Mate
dosyaları önceki nottan sonra eklenmişti. Bu devam oturumunda:

- `/tmp/argus-llamacpp-v050` ve test modeli kayıptı. Sabit kaynak revision arşivi
  `scratch/llama.cpp-v050` içine açıldı; patch uygulandı ve CPU `llama-server`
  derlemesi tamamlandı. Bu yerel Git dizininde upstream tarihçesi yok;
  binary commit etiketi `unknown`, kaynak revision yukarıdaki sabit arşivdir.
  CUDA derlemesi bu oturumda yeniden yapılmadı.
- Test modeli `scratch/models/stories15M.gguf`, aynı SHA-256 ile doğrulandı.
  UI-Mate model ve projector boyut/hash'leri de manifestle doğrulandı.
- Native worker koordinasyonu her op çağrısında yenileniyor ve son worker ile
  bırakılıyor. Kalıcı KV-adresi map'inin eski sayaç/sızıntı sorunu giderildi.
- FP32 V dönüşümü tamamlandı; blok ayarı sıkı doğrulanıyor ve gerçek hücre
  sayısıyla sınırlandırılıyor. Kernel testleri her 1/4/16/4 worker koşusunun
  sonucunu kontrol ediyor; FP32/FP16/BF16/Q8/Q4 için yedi senaryo geçti.
- Geniş GPU/core suite: **375 passed, 5 skipped, 5 deselected**. Native env
  ayarları olmayan bu koşudan sonra native suite ayrıca **5 passed** verdi:
  koordinasyon, kernel, FP32/FP16 prefill/decode/crop/state ve CPU sunucusunda
  stock–host–paged 64-token çıktı eşitliği. Geçici test sunucuları kapatıldı.
- Q8/Q4 model senaryoları denendi ancak stories15M head_dim=48, GGML'nin
  32 elemanlı quantized bloklarıyla uyumsuz olduğundan stock context kurulamadı.
  Bu senaryolar fixture'a eklenmedi; Q8/Q4 gerçek model kabulü açık.
- `ARGUS_KV_RESIDENT_BYTES` yalnız hedeftir: mevcut mmap/advice yolu kesin
  resident/pinned/staging bütçesi sağlamaz. Örneklenmiş mincore peak'i gerçek
  peak değildir; tahliye sayaçları doğrulanmış fiziksel I/O miktarı değildir.
  M2 ve diğer açık v0.5 kabul kapıları kapanmadı. Python PageStore hâlâ ayrı.
- `scratch/kv` bu oturumda `/dev/sda2`, btrfs üzerinde. NVMe sonucu sayılmadı.
  Derleme aracı `/tmp/argus-v050-build-tools/cmake/data/bin/cmake` altında.
  Son `git diff --check` temiz. Kullanıcı değişiklikleri korunuyor; commit/push yok.

Tekrar çalıştırma:

```sh
ARGUS_LLAMA_CPP_DIR="$PWD/scratch/llama.cpp-v050" \
ARGUS_TEST_GGUF="$PWD/scratch/models/stories15M.gguf" \
  .venv/bin/python -m pytest -q tests/test_native_llama_paged.py
```

Sıradaki iş: M1/M2/M4 bağlantısında kesin bütçeli native storage ve staging;
mevcut `madvise` tahliye hedefini hard limit diye adlandırma. Codebase graph bu
oturumda erişilebildi: proje adı
`home-zwannfrederick-Masac3bcstc3bc-Sektor-Coding-mamba-fix`; yeniden index gerekmedi.

## Neden duruldu / yetki

Kullanıcı ayrı session açmak için durdurdu ve planın güncellenmesini istedi.
Teknik blocker, derleme bekleyişi veya açıklığa kavuşturulacak kapsam yok.
Önceki ara sonuçlarda turun kapatılması gereksizdi; yeni oturum ara test geçince
yeniden “devam” istemesin. Kapsam **llama.cpp sahipliği dahil tüm v0.5 hedefleri**.
Ancak yeni dış etki (mesaj gönderme, servis açma, release/push) izni varsayılmasın.

Ana plan: [argus-v0.5.0.md](../../plans/argus-v0.5.0.md).
Neo güvenlik/servis durumu: [neo-handoff.md](../plans/neo-handoff.md).

## Tam olarak kaldığımız yer

CUDA `llama-server` ve `llama-cli` derlendi. GPU smoke da **geçti**:
RTX 3050 Ti, 7/7 model katmanı offload, 57.95 MiB CUDA model buffer;
stock ve ARGUS 16 greedy token aynı, prompt cache kapalı.
ARGUS host KV mapping mevcut, 1,769,472 byte tahsis, teardown live byte 0.

**Model GPU'da; KV ARGUS'un host mmap buffer'ında.** `-nkvo` kullanılıyor.
Bu GPU-resident KV, GPU attention, PageStore bağlantısı veya tamamlanmış tiering
kanıtı değildir. Mevcut attention grafiği ve tensor düzeni GGML'nindir.
CPU native test ayrıca 32,000 logitte prefill + state restore sonrası 8 decode
adımında maksimum fark 0 ve 1-byte bütçeyle context oluşturma reddi gösterdi.

Kanıt: [ownership JSON](../measurements/v050-llama-host-ownership-2026-09-15.json).

## Yeni oturumun ilk işi

1. Dirty worktree'yi koru; aşağıdaki geçici dosyaların hâlâ mevcut olduğunu kontrol et.
   Derleme bitmiştir; gerekmedikçe yeniden build/download yapma.
2. Native bağlantıyı izle: `integrations/llama.cpp/host-kv.patch`,
   `argus_cache/csrc/ggml_host_buffer.cpp`, upstream `src/llama-kv-cache.cpp` ve
   attention read/write graph. Python `core/page_store.py` ayrı bir prototiptir.
3. **M1/M2/M4 birleşimindeki eksik işi uygula:** native sayfa kimliği/yerleşimi ile
   allocation → append/write → attention read → crop/reset/state → teardown
   yaşam döngüsünü bağla. Önce küçük full-attention modelde zorunlu spill ve
   parity testi; ardından GPU/RAM/disk ve sınırlı staging/prefetch. Sadece mmap
   buffer'a “NVMe tiering” adını vermek kabul değildir.
4. Desteklenmeyen contract fail-closed kalsın. Qwen3.5 attention registry'ye
   yalnız isim eklemek yetmez: linear/recurrent Cache lifecycle eksik. M3 sonrası
   gerçek context/quality merdiveni, en son UI-Mate workload'u.

## Kod ve test durumu

- PageStore: codec+placement sahipliği, generation kontrolü, pinned sayfa koruma,
  oldest-position spill, decode boyunca lock. Native runtime'a bağlı değil.
- DiskBlockPool: sınırlı slot, checksum, atomik replace, tek outstanding prefetch.
  Kapasite payload sınırıdır; RSS/OS page cache sınırı değildir.
- Direct engine: kısmi FP16 düzeltmesi, BF16, placement seçimi, logical-position
  sliding window; runtime/model desteği ayrı doğrulanmalı.
- HF GGML profilinde mevcut C++ streaming attention açıldı; Q8/Q4/Q2/1-bit parity.
- HybridTopology nested text config ve explicit layer_types okuyor; core'a model
  adı eklenmedi. Bu gerçek UI-Mate inference desteği değildir.
- Son geniş GPU suite: `374 passed, 1 skipped, 5 deselected` (40.37 saniye).
  vLLM kurulu olmadığı için bir probe skip. Beş `ollama_live` ayrı tutuldu;
  önceki tam koşuda 35B model yükleme 300 saniyede timeout verdi.
- Geniş suite sonrasında server kontrolüne GPU parametresi/verbose log kanıtı
  eklendi ve GPU server testi geçti; geniş suite tekrar çalıştırılmadı.
- Sentetik 1,048,576 token disk smoke geçti: yalnız 1 layer / 1 KV head / D4.
  Gerçek 1M model veya NVMe performans sonucu değildir.
- Son `git diff --check` temiz. Commit/push/tag yok, paket hâlâ 0.4.0.
- Ayrı agent code review yapılmadı; native yaşam döngüsü/hata yolları hâlâ inceleme
  gerektirir. Özellikle move sonrası kaynak free hatasının orphan slot bırakması
  ve staging/resident bütçeleri sonraki kontrolde ele alınmalı.

## Ortam / tekrar çalıştırma

Çalışma dizini: `/home/zwannfrederick/Masaüstü/Sektor/Coding/mamba fix`.
Python `.venv/bin/python`, torch 2.12.0+cu130, Python 3.14.7.
NVIDIA sürücü 610.57.04. Sandbox CUDA aygıtlarını göstermiyor; bu GPU yok demek
değildir. GPU testlerini onaylı `require_escalated` erişimiyle çalıştır.

```sh
OMP_NUM_THREADS=2 .venv/bin/python -m pytest -q -k 'not ollama_live'
.venv/bin/python tests/cpp/check_llama_server_ownership.py /tmp/argus-llamacpp-v050/build/bin/llama-server /tmp/argus-stories15M.gguf scratch 99
/tmp/argus-llama-host-test /tmp/argus-stories15M.gguf
/tmp/argus-host-buffer-test
```

Geçici build/artifact yolları (session/reboot sonrasında kaybolabilir):

- `/tmp/argus-llamacpp-v050`: upstream SHA `54315813269112dd0baed7112ec87ad93a8218ca`,
  ARGUS patch uygulanmış. `build/bin/` CUDA, `build-cpu/bin/` CPU executable/library.
- `/tmp/argus-v050-build-tools/cmake/data/bin/cmake`: build-only CMake.
- `/tmp/argus-stories15M.gguf`: SHA256
  `61b50d457809a5194818fd22e6724b456cd7bb9a6264c52c8110684c53f3704a`.
  Kaynak `ggml-org/tiny-llamas`, revision
  `99dd1a73db5a37100bd4ae633f4cfce6560e1567`, dosya `stories15M.gguf`.
- Kalıcı yeniden build yönergesi: `integrations/llama.cpp/README.md`.
- Test server süreçleri testin finally bloğunda kapatıldı; bilinen aktif test veya
  build session kalmadı. Neo/Hermes servisleri başlatılmadı.

`/tmp` RAM-backed olabilir; gerçek storage ölçümünde mount/device türünü yeniden
doğrula. Workspace testini kendiliğinden NVMe benchmark sayma.

## UI-Mate ve repo korumaları

Kullanıcı modeli verdi: `https://huggingface.co/bartowski/tencent_UI-Mate-9B-GGUF`.
Revision, Q4_K_M ve f16 mmproj boyut/hash'leri
`plans/ui-mate-v050-artifact-manifest.json` içinde. Dosyalar indirilmedi.
Q4 + projector yaklaşık 6.83 GB; tümünü 4 GB VRAM'e sığıyor diye sunma.
Official config Qwen3.5 hybrid: 32 layer, 8 full + 24 linear, 16 query/4 KV head,
D256, native context 262144. 1M position extension ayrıca kalite kapısıdır.

Tüm v0.5 değişiklikleri uncommitted. `.pytest-cache/` ve `neo-mobile/` önceden
mevcut kullanıcı dosyalarıdır; silme/revert etme. Diğer scratch modelleri koru.
Codebase-memory graph index denemesi kaynak export riski nedeniyle auto-review
tarafından reddedildi; aynı export'u dolanma. Graph kullanılamazsa yerel rg/read
fallback kullan ve yeni oturumdaki AGENTS talimatlarını kontrol et.
